@model List<ResultPricingDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="page-content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Pricing Period List</h4>
                        <p class="card-subtitle mb-4">
                            You can manage your pricing period here. You can add, list, delete and edit pricing period from this Page.
                        </p>
                        <div class="table-responsive">

                            <table class="table mb-0">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Period</th>
                                        <th>Operatıons</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@item.PricingId</td>
                                            <td>@item.PricingPeriod</td>
                                            <td>
                                                <a href="/Admin/AdminPricing/UpdatePricing/@item.PricingId" class="btn btn-outline-warning">Update</a>
                                                <a href="/Admin/AdminPricing/DeletePricing/@item.PricingId" class="btn btn-outline-danger">Delete</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                            <br />
                            <a href="/Admin/AdminPricing/CreatePricing" class="btn btn-outline-info">Add Pricing Period</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>





